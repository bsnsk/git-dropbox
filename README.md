# git-dropbox

A bash script to backup your git repositories in your Dropbox folder.

## Install
```bash
./install.sh 
```

## Usage

```bash
$ cd mygitproject
$ git-dropbox create
$ git-dropbox push
$ git-dropbox pull
```
